<div class = "productContainer">
{{#each products}}
    <div class ="productCard">
        <p>{{this.title}}</p>
        <p>{{this.price}}</p>
        <img src={{this.thumbnail}}>
    </div>
{{/each}}

</div>

